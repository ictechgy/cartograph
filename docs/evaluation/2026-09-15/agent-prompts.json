{
  "normalization": "Only the absolute evaluation workspace prefix is replaced by EVAL_ROOT.",
  "fork_turns": "none",
  "prompts": [
    {
      "agent": "trial_af_search",
      "role": "explore",
      "arm": "search",
      "prompt": "Experimental read-only code review task. Use ONLY ordinary source inspection/search tools (rg, sed, Python file reads) and git. Do NOT use Cartograph, Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/alamofire. You are not alone; no writes. First tool: record current UTC timestamp; last tool: record current UTC timestamp. Scope Source/**/*.swift production code compiled on macOS, exclude tests/examples/inactive #if branches. Two independent requests: (1) If Instant.init() at Source/Core/Instant.swift:42 changes, list every immediate consuming function/property declaration in scope. (2) If Request.processNextResponseSerializer() at Source/Core/Request.swift:613 changes, list every immediate consuming function/property declaration in scope. Enumerate direct references only, not transitive callers or containing types. Collapse multiple references inside one consumer. Include local nested functions as separate consumers where present. Return JSON only: {startedAt,finishedAt,results:[{id:'af-clock' or 'af-serializer',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths and 1-based declaration lines. Determine completeness from code; no supplied expected count. Aim complete within 4 minutes."
    },
    {
      "agent": "trial_kf_cartograph",
      "role": "explore",
      "arm": "cartograph",
      "prompt": "Experimental read-only code review task. Use ordinary source inspection/search tools (rg, sed, Python file reads), git, AND Cartograph. Do not use Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/kingfisher. You are not alone; no writes. First tool: record current UTC timestamp; last tool: record current UTC timestamp. Read applicable project AGENTS.md. Scope Sources/**/*.swift production code compiled on macOS, exclude tests/examples/inactive #if branches. Two independent requests: (1) If KingfisherError.isTaskCancelled at Sources/General/KingfisherError.swift:412 changes, list every immediate consuming function/property declaration. (2) If KingfisherManager.deliverOriginalCacheHit at Sources/General/KingfisherManager.swift:974 changes, list every immediate consuming function/property declaration. Direct references only, not transitive callers or containing types. Collapse repeated refs within a consumer; include local nested functions separately. Cartograph is installed; from repo use `cartograph impact isTaskCancelled --index-store .build/out --include 'Sources/**' --depth 1 --limit 1000 --format json` and similarly `deliverOriginalCacheHit`; verify interpretation against source. Return JSON only: {startedAt,finishedAt,results:[{id:'kf-cancel' or 'kf-cache',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths,1-based declaration lines. No expected count supplied. Aim complete within4 minutes."
    },
    {
      "agent": "trial_ap_search",
      "role": "explore",
      "arm": "search",
      "prompt": "Experimental read-only code review task. Use ONLY ordinary source inspection/search tools (rg, sed, Python file reads) and git. Do NOT use Cartograph, Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/argument-parser. You are not alone; no writes. First tool record current UTC timestamp; last tool record current UTC timestamp. Scope Sources/ArgumentParser/**/*.swift and Sources/ArgumentParserToolInfo/**/*.swift production macOS code; exclude tests/examples/tools/test helpers/inactive #if branches. Two requests: (1) If getHelpBanner() at Sources/ArgumentParser/Usage/HelpGenerator.swift:486 changes, list all immediate consuming function/property declarations. (2) If CommandInfoV0.bashCompletionScript at Sources/ArgumentParser/Completions/BashCompletionsGenerator.swift:21 changes, list all immediate consuming function/property declarations. Direct references only, not transitive callers or containing types; distinguish same-named properties on other types. Collapse repeated refs per consumer and include local nested functions separately. Return JSON only: {startedAt,finishedAt,results:[{id:'ap-banner' or 'ap-bash',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths,1-based declaration lines. No expected count supplied. Aim complete within 4 minutes."
    },
    {
      "agent": "trial_af_cartograph",
      "role": "explore",
      "arm": "cartograph",
      "prompt": "Experimental read-only code review task. Use ordinary source inspection/search tools (rg, sed, Python file reads), git, AND Cartograph. Do not use Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/alamofire. You are not alone; no writes. First tool record current UTC timestamp; last tool record current UTC timestamp. Scope Source/**/*.swift production macOS code, exclude tests/examples/inactive #if branches. Two requests: (1) If Instant.init() at Source/Core/Instant.swift:42 changes, list all immediate consuming function/property declarations. (2) If Request.processNextResponseSerializer() at Source/Core/Request.swift:613 changes, list all immediate consuming function/property declarations. Direct references only, not transitive callers or containing types. Collapse repeated refs per consumer; include local nested functions separately. Cartograph is installed; from repo use `cartograph impact 's:9Alamofire7InstantVACycfc' --index-store .build/out --include 'Source/**' --depth 1 --limit 1000 --format json`, and similarly `processNextResponseSerializer`; verify interpretation against source. Return JSON only: {startedAt,finishedAt,results:[{id:'af-clock' or 'af-serializer',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths,1-based declaration lines. No expected count supplied. Aim complete within 4 minutes."
    },
    {
      "agent": "trial_kf_search",
      "role": "explore",
      "arm": "search",
      "prompt": "Experimental read-only code review task. Use ONLY ordinary source inspection/search tools (rg, sed, Python file reads) and git. Do NOT use Cartograph, Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/kingfisher. You are not alone; no writes. First tool record current UTC timestamp; last tool record current UTC timestamp. Read applicable project AGENTS.md. Scope Sources/**/*.swift production macOS code, exclude tests/examples/inactive #if branches. Two requests: (1) If KingfisherError.isTaskCancelled at Sources/General/KingfisherError.swift:412 changes, list every immediate consuming function/property declaration. (2) If KingfisherManager.deliverOriginalCacheHit at Sources/General/KingfisherManager.swift:974 changes, list every immediate consuming function/property declaration. Direct references only, not transitive callers or containing types. Collapse repeated refs within a consumer; include local nested functions separately. Return JSON only: {startedAt,finishedAt,results:[{id:'kf-cancel' or 'kf-cache',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths,1-based declaration lines. No expected count supplied. Aim complete within 4 minutes."
    },
    {
      "agent": "trial_ap_cartograph",
      "role": "explore",
      "arm": "cartograph",
      "prompt": "Experimental read-only code review task. Use ordinary source inspection/search tools (rg, sed, Python file reads), git, AND Cartograph. Do not use Periphery, SourceKit/LSP, web, builds, other agents, or any files outside EVAL_ROOT/repos/argument-parser. You are not alone; no writes. First tool record current UTC timestamp; last tool record current UTC timestamp. Scope Sources/ArgumentParser/**/*.swift and Sources/ArgumentParserToolInfo/**/*.swift production macOS code; exclude tests/examples/tools/test helpers/inactive #if branches. Two requests: (1) If getHelpBanner() at Sources/ArgumentParser/Usage/HelpGenerator.swift:486 changes, list all immediate consuming function/property declarations. (2) If CommandInfoV0.bashCompletionScript at Sources/ArgumentParser/Completions/BashCompletionsGenerator.swift:21 changes, list all immediate consuming function/property declarations. Direct references only, not transitive callers or containing types; distinguish same-named properties. Collapse repeated refs per consumer; include local nested functions separately. Cartograph is installed; from repo use `cartograph impact getHelpBanner --index-store .build/out --include 'Sources/ArgumentParser/**' 'Sources/ArgumentParserToolInfo/**' --depth 1 --limit 1000 --format json`. For bashCompletionScript use query to resolve its same-named candidates by declaration location, then impact by USR with same flags. Verify interpretation against source. Return JSON only: {startedAt,finishedAt,results:[{id:'ap-banner' or 'ap-bash',consumers:[{path,line,name,referenceLines:[]}],limitations:[]}],commands:[...]} with project-relative paths,1-based declaration lines. No expected count supplied. Aim complete within 4 minutes."
    }
  ]
}
